confirm(`Escolha dois números e veja quais são pares entre eles.`);

var num1 = Number(prompt('Digite um número'));

var num2 = Number(prompt('Digite um segundo número'));

while (num1 <= num2) {

    if (num1 % 2 === 0) {
    console.log(num1);
    }

    num1++;
    }
